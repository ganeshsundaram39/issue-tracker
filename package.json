{
  "name": "issue-tracker",
  "version": "0.0.0",
  "scripts": {
    "dev": "cd issue-tracker-frontend && yarn start:dev",
    "prod": "cd issue-tracker-frontend && yarn start:prod",
    "server": "cd issue-tracker-backend && yarn start",
    "format": "prettier --write ."
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
